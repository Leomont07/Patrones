class OldNotificationSystem {
    sendSMS(phoneNumber, message) {
      console.log(`Enviando SMS a ${phoneNumber}: "${message}" usando el sistema antiguo.`);
    }
  }
  
  module.exports = OldNotificationSystem;
  